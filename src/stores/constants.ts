export enum API {
  REGISTER = `/register/`,
  LOGIN = "/login/",
  LOGOUT = "/logout",
  CREATE_VERIFICATION_CODE = "/token/verify/",
  CREATE_REFRESH_TOKEN = "/token/refresh/",
  TODOS = "/todos/",
}
